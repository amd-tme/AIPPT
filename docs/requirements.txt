# Build-time dependencies for the Sphinx HTML docs.
# Consumed only by the `docs` stage in the Dockerfile (sphinx-build) — not part
# of the runtime image or requirements.txt. Versions track the known-good local
# build (Sphinx 8.1.3 / sphinx-rtd-theme 3.1.0 / myst-parser 4.0.1).
sphinx>=8.1,<9
sphinx-rtd-theme>=3.0,<4
myst-parser>=4.0,<5
